# ctools

moveFilter: o==>d  f have

    -o : source address

    -f : filter file

    -d : dest address
 

movenFilter: o==>d f no have

    -o : source address

    -f : filter file

    -d : dest address
